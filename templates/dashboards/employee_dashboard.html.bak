
{% extends "base.html" %}
{% load custom_tags %}
{% block title %}Employee Dashboard{% endblock %}
{% block content %}
<style>
  .dashboard-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 2rem;
  }
  .dashboard-header img {
    height: 48px;
    width: auto;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  }
  .dashboard-header h2 {
    font-weight: 700;
    font-size: 2rem;
    color: #1a202c;
    margin: 0;
  }
  .modern-card {
    background: #fff;
    border-radius: 18px;
    box-shadow: 0 2px 16px rgba(0,0,0,0.07);
    padding: 2rem 1.5rem;
    margin-bottom: 1.5rem;
    transition: box-shadow 0.2s;
  }
  .metric-card {
    min-height: 220px;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 0.35rem;
  }
  .modern-card:hover {
    box-shadow: 0 4px 24px rgba(0,0,0,0.12);
  }
  .modern-progress {
    height: 18px;
    border-radius: 9px;
    background: #e9ecef;
    overflow: visible;  /* allow celebration sparks to show */
    position: relative;
  }
  .modern-progress-bar {
    height: 100%;
    border-radius: 9px;
    transition: width 0.6s cubic-bezier(.4,0,.2,1);
    font-weight: 600;
    font-size: 0.95rem;
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .bar-green { background: linear-gradient(90deg, #48bb78 0%, #38a169 100%); }
  .bar-blue { background: linear-gradient(90deg, #4299e1 0%, #3182ce 100%); }
  .bar-amber { background: linear-gradient(90deg, #f6ad55 0%, #ed8936 100%); }
  .modern-progress-bar-success {
    background: linear-gradient(90deg, #38b2ac 0%, #319795 100%);
  }
  .modern-progress-bar-info {
    background: linear-gradient(90deg, #4299e1 0%, #3182ce 100%);
  }
  .dashboard-section-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: #2d3748;
    margin-bottom: 1rem;
    margin-top: 2rem;
  }
  .product-card {
    background: #f7fafc;
    border-radius: 14px;
    box-shadow: 0 1px 6px rgba(0,0,0,0.05);
    padding: 1.2rem 0.8rem;
    text-align: center;
    margin: 0.5rem;
    min-width: 160px;
    max-width: 200px;
  }
  .product-card h6 {
    font-weight: 600;
    color: #4a5568;
    margin-bottom: 0.5rem;
  }
  .product-card h5 {
    font-weight: 700;
    color: #2b6cb0;
    margin: 0;
  }
  .dashboard-link {
    display: inline-flex;
    align-items: center;
    padding: 0.7rem 1.5rem;
    border-radius: 12px;
    background: linear-gradient(90deg, #f6e05e 0%, #ecc94b 100%);
    color: #2d3748;
    font-weight: 600;
    text-decoration: none;
    box-shadow: 0 1px 6px rgba(0,0,0,0.07);
    transition: background 0.2s;
  }
  .dashboard-link:hover {
    background: linear-gradient(90deg, #ecc94b 0%, #f6e05e 100%);
    color: #1a202c;
  }
  .salary-meter { margin-top: 0.75rem; }
  .salary-track { position: relative; height: 12px; border-radius: 8px; background: #e2e8f0; overflow: hidden; }
  .salary-fill { height: 100%; background: linear-gradient(90deg, #4299e1 0%, #3182ce 100%); border-radius: 8px; }
  .salary-line { position: absolute; top: -4px; width: 3px; height: 20px; background: #ed8936; border-radius: 3px; box-shadow: 0 0 0 2px rgba(237,137,54,0.15); }
  @keyframes pulseGlow {
    0% { box-shadow: 0 0 0 0 rgba(66,153,225,0.45); }
    100% { box-shadow: 0 0 0 16px rgba(66,153,225,0); }
  }
  @keyframes riseFade {
    0% { opacity: 1; transform: translateY(0) scale(0.9); }
    100% { opacity: 0; transform: translateY(-12px) scale(1.35); }
  }
  .pulse-glow { animation: pulseGlow 1s ease-out; }
  .spark-pop {
    position: absolute;
    top: 0;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: linear-gradient(135deg, #f6e05e 0%, #ed8936 100%);
    animation: riseFade 0.9s ease-out forwards;
    pointer-events: none;
  }
  .modern-card, .product-card {
    border: 1px solid rgba(236, 201, 75, 0.18);
  }
  .stat-list { display:flex; flex-direction:column; gap:0.35rem; width:100%; }
  .stat-item { display:flex; justify-content:space-between; align-items:center; padding:0.35rem 0.55rem; background:#f8fafc; border:1px solid #edf2f7; border-radius:10px; font-size:0.95rem; color:#4a5568; }
  .stat-item strong { color:#2d3748; font-weight:700; }
  .stat-link { text-decoration:none; color:inherit; display:block; }
  .stat-link:hover .stat-item { background:#fffde7; border-color:#f6e05e; }
  .card-subtitle { font-weight:700; color:#2d3748; margin:0.1rem 0 0.5rem 0; }
  @media (max-width: 900px) {
    .dashboard-header h2 { font-size: 1.3rem; }
    .modern-card { padding: 1rem 0.5rem; }
    .dashboard-section-title { font-size: 1rem; }
    .product-card { padding: 0.8rem 0.3rem; }
    .row { gap: 0.5rem; }
  }
  @media (max-width: 600px) {
    .dashboard-header { flex-direction: column; gap: 0.5rem; }
    .dashboard-header h2 { font-size: 1.05rem; }
    .modern-card { padding: 0.7rem 0.1rem; }
    .dashboard-section-title { font-size: 0.98rem; }
    .product-card { padding: 0.5rem 0.1rem; font-size: 0.92rem; }
    .dashboard-link { font-size: 0.95rem; padding: 0.45rem 0.7rem; }
    .row { gap: 0.3rem; }
  }
</style>

<div class="row" style="display:flex;flex-wrap:wrap;gap:1rem;align-items:stretch;">
  <div class="col-md-6 mb-3" style="flex:1 1 300px;min-width:220px;">
    <div class="modern-card text-center metric-card">
      <h5 style="color:#319795;font-weight:600;">Total Sales (This Month)</h5>
      <h2 style="color:#38b2ac;font-weight:700;">₹{{ total_sales }}</h2>
    </div>
  </div>
  <div class="col-md-6 mb-3" style="flex:1 1 300px;min-width:220px;">
    <div class="modern-card metric-card">
      <div class="text-center">
        <h5 style="color:#3182ce;font-weight:600;">Total Points (This Month)</h5>
        <h2 style="color:#4299e1;font-weight:700;" id="points-counter" data-points="{{ total_points }}">0</h2>
        <div class="text-muted" style="font-size:0.95rem;">Pending points awaiting approval: {{ pending_points }}</div>
        {% if salary_points %}
        <div class="salary-meter mt-3" data-salary-meter data-salary="{{ salary_points }}" data-salary-ratio="{{ salary_ratio|floatformat:0 }}" data-points-ratio="{{ points_ratio|floatformat:0 }}">
          <div class="d-flex justify-content-between" style="font-size:0.9rem;color:#4a5568;">
            <span>Salary point: {{ salary_points|floatformat:0 }}</span>
            <span>Extra: {{ extra_points|floatformat:0 }}</span>
          </div>
          <div class="salary-track mt-2">
            <div class="salary-fill" data-progress="{{ points_ratio|floatformat:0 }}" style="width: {{ points_ratio|floatformat:0 }}%;"></div>
            <div class="salary-line" data-salary-ratio="{{ salary_ratio|floatformat:0 }}" style="left: {{ salary_ratio|floatformat:0 }}%;"></div>
          </div>
          <div class="text-muted" style="font-size:0.85rem;margin-top:0.35rem;">Beyond salary = incentive points</div>
        </div>
        {% endif %}
      </div>
    </div>
  </div>
  <div class="col-12 mb-3" style="flex:1 1 100%;min-width:280px;">
    <div class="modern-card" style="text-align:left;">
      {% include "dashboards/_followup_calendar.html" %}
    </div>
  </div>
</div>

<div class="dashboard-section-title">Product Breakup</div>
<div class="row" style="display:flex;flex-wrap:wrap;gap:1rem;align-items:stretch;">
  <div class="col-xl-3 col-lg-4 col-md-6" style="flex:1 1 260px;min-width:240px;">
    <div class="modern-card" style="height:100%;">
      <div class="card-subtitle">My Sales by product</div>
      <div class="stat-list">
        {% for item in product_sales_breakup %}
          <a class="stat-link" href="{% url 'clients:all_sales' %}?product={{ item.product|urlencode }}&start_date={{ month_start|date:'Y-m-d' }}&end_date={{ month_end|date:'Y-m-d' }}">
            <div class="stat-item"><span>{{ item.label }}</span><strong>₹{{ item.amount|floatformat:0 }}</strong></div>
          </a>
        {% empty %}
          <div class="text-muted">No sales yet.</div>
        {% endfor %}
      </div>
    </div>
  </div>

  <div class="col-xl-3 col-lg-4 col-md-6" style="flex:1 1 260px;min-width:240px;">
    <div class="modern-card" style="height:100%;">
      <div class="card-subtitle">My Points by product</div>
      <div class="stat-list">
        {% for item in product_point_breakup %}
          <a class="stat-link" href="{% url 'clients:all_sales' %}?product={{ item.product|urlencode }}&start_date={{ month_start|date:'Y-m-d' }}&end_date={{ month_end|date:'Y-m-d' }}">
            <div class="stat-item"><span>{{ item.label }}</span><strong>{{ item.points|floatformat:0 }}</strong></div>
          </a>
        {% empty %}
          <div class="text-muted">No points yet.</div>
        {% endfor %}
      </div>
    </div>
  </div>

  {% if show_company_sections %}
  <div class="col-xl-3 col-lg-4 col-md-6" style="flex:1 1 260px;min-width:240px;">
    <div class="modern-card" style="height:100%;">
      <div class="card-subtitle">Overall Sales by product</div>
      <div class="stat-list">
        {% for item in overall_product_sales_breakup %}
          <a class="stat-link" href="{% url 'clients:all_sales' %}?product={{ item.product|urlencode }}&start_date={{ month_start|date:'Y-m-d' }}&end_date={{ month_end|date:'Y-m-d' }}">
            <div class="stat-item"><span>{{ item.label }}</span><strong>₹{{ item.amount|floatformat:0 }}</strong></div>
          </a>
        {% empty %}
          <div class="text-muted">No sales yet.</div>
        {% endfor %}
      </div>
    </div>
  </div>

  <div class="col-xl-3 col-lg-4 col-md-6" style="flex:1 1 260px;min-width:240px;">
    <div class="modern-card" style="height:100%;">
      <div class="card-subtitle">Overall Points by product</div>
      <div class="stat-list">
        {% for item in overall_product_point_breakup %}
          <a class="stat-link" href="{% url 'clients:all_sales' %}?product={{ item.product|urlencode }}&start_date={{ month_start|date:'Y-m-d' }}&end_date={{ month_end|date:'Y-m-d' }}">
            <div class="stat-item"><span>{{ item.label }}</span><strong>{{ item.points|floatformat:0 }}</strong></div>
          </a>
        {% empty %}
          <div class="text-muted">No points yet.</div>
        {% endfor %}
      </div>
    </div>
  </div>
  {% endif %}
</div>

<div class="dashboard-section-title">Product-wise Sales (This Month)</div>
<div class="row" style="display:flex;flex-wrap:wrap;gap:1rem;justify-content:center;align-items:stretch;">
  <div class="col-md-2 col-6" style="flex:0 1 180px;min-width:150px;max-width:200px;">
    <div class="product-card">
      <h6>SIP</h6>
      <h5>₹{{ sip_sales }}</h5>
    </div>
  </div>
  <div class="col-md-2 col-6" style="flex:0 1 180px;min-width:150px;max-width:200px;">
    <div class="product-card">
      <h6>Lumpsum</h6>
      <h5>₹{{ lumsum_sales }}</h5>
    </div>
  </div>
  <div class="col-md-2 col-6" style="flex:0 1 180px;min-width:150px;max-width:200px;">
    <div class="product-card">
      <h6>Life Insurance</h6>
      <h5>₹{{ life_sales }}</h5>
    </div>
  </div>
  <div class="col-md-2 col-6" style="flex:0 1 180px;min-width:150px;max-width:200px;">
    <div class="product-card">
      <h6>Health Insurance</h6>
      <h5>₹{{ health_sales }}</h5>
    </div>
  </div>
  <div class="col-md-2 col-6" style="flex:0 1 180px;min-width:150px;max-width:200px;">
    <div class="product-card">
      <h6>Motor Insurance</h6>
      <h5>₹{{ motor_sales }}</h5>
    </div>
  </div>
  <div class="col-md-2 col-6" style="flex:0 1 180px;min-width:150px;max-width:200px;">
    <div class="product-card">
      <h6>PMS</h6>
      <h5>₹{{ pms_sales }}</h5>
    </div>
  </div>
</div>

<div class="dashboard-section-title">Progress vs Targets</div>
<div class="row" style="display:flex;flex-wrap:wrap;gap:1rem;">
  <div class="col-md-6" style="flex:1 1 320px;min-width:260px;">
    <div class="modern-card">
      <h6 style="font-weight:700;color:#319795;">Daily Progress</h6>
      {% for target in daily_targets %}
        <div class="mb-3">
          <div style="display:flex;justify-content:space-between;font-weight:600;color:#2d3748;">
            <span>{{ target.product }}</span>
            <span>₹{{ target.achieved|floatformat:0 }} / ₹{{ target.target_value|floatformat:0 }}</span>
          </div>
          <div class="modern-progress mt-1">
            <div class="modern-progress-bar modern-progress-bar-success" data-progress="{{ target.progress|floatformat:0 }}">{{ target.progress|floatformat:0 }}%</div>
          </div>
        </div>
      {% empty %}
        <p class="text-muted mb-0">No daily targets set.</p>
      {% endfor %}
    </div>
  </div>
  <div class="col-md-6" style="flex:1 1 320px;min-width:260px;">
    <div class="modern-card">
      <h6 style="font-weight:700;color:#3182ce;">Monthly Progress</h6>
      {% for target in monthly_targets %}
        <div class="mb-3">
          <div style="display:flex;justify-content:space-between;font-weight:600;color:#2d3748;">
            <span>{{ target.product }}</span>
            <span>₹{{ target.achieved|floatformat:0 }} / ₹{{ target.target_value|floatformat:0 }}</span>
          </div>
          <div class="modern-progress mt-1">
            <div class="modern-progress-bar modern-progress-bar-info" data-progress="{{ target.progress|floatformat:0 }}">{{ target.progress|floatformat:0 }}%</div>
          </div>
        </div>
      {% empty %}
        <p class="text-muted mb-0">No monthly targets set.</p>
      {% endfor %}
    </div>
  </div>
</div>

{% if show_company_sections %}
<hr class="my-4" />
<h4 style="font-weight:700;color:#2d3748;">Company Progress vs Targets</h4>
<div class="row" style="display:flex;flex-wrap:wrap;gap:1rem;">
  <div class="col-md-6" style="flex:1 1 320px;min-width:260px;">
    <div class="modern-card">
      <h6 style="font-weight:700;color:#38a169;">Daily Progress</h6>
      {% for row in overall_daily_progress %}
        <div class="mb-3">
          <div style="display:flex;justify-content:space-between;font-weight:600;color:#2d3748;">
            <span>{{ row.product }}</span>
            <span>₹{{ row.achieved|floatformat:0 }} / ₹{{ row.target|floatformat:0 }}</span>
          </div>
          <div class="modern-progress mt-1">
            <div class="modern-progress-bar bar-green" data-progress="{{ row.progress|floatformat:0 }}">{{ row.progress|floatformat:0 }}%</div>
          </div>
        </div>
      {% empty %}
        <p class="text-muted mb-0">No daily targets set.</p>
      {% endfor %}
    </div>
  </div>
  <div class="col-md-6" style="flex:1 1 320px;min-width:260px;">
    <div class="modern-card">
      <h6 style="font-weight:700;color:#3182ce;">Monthly Progress</h6>
      {% for row in overall_monthly_progress %}
        <div class="mb-3">
          <div style="display:flex;justify-content:space-between;font-weight:600;color:#2d3748;">
            <span>{{ row.product }}</span>
            <span>₹{{ row.achieved|floatformat:0 }} / ₹{{ row.target|floatformat:0 }}</span>
          </div>
          <div class="modern-progress mt-1">
            <div class="modern-progress-bar bar-blue" data-progress="{{ row.progress|floatformat:0 }}">{{ row.progress|floatformat:0 }}%</div>
          </div>
        </div>
      {% empty %}
        <p class="text-muted mb-0">No monthly targets set.</p>
      {% endfor %}
    </div>
  </div>
</div>

<h4 style="font-weight:700;color:#2d3748;" class="mt-4">Monthly Employee Performance (Product-wise)</h4>
<div class="row" style="display:flex;flex-wrap:wrap;gap:1rem;">
  {% for row in monthly_employee_product %}
    <div class="col-md-4" style="flex:1 1 260px;min-width:240px;">
      <div class="modern-card" style="padding:1rem 0.8rem;">
        <div style="font-weight:700;color:#2b6cb0; margin-bottom:0.4rem;">{{ row.employee }}</div>
        {% for p in row.products %}
          <div class="mb-2">
            <div style="display:flex;justify-content:space-between;font-size:0.9rem;color:#4a5568;">
              <span>{{ p.product }}</span>
              <span>₹{{ p.achieved|floatformat:0 }} / ₹{{ p.target|floatformat:0 }}</span>
            </div>
            <div class="modern-progress mt-1">
              <div class="modern-progress-bar bar-blue" data-progress="{{ p.progress|floatformat:0 }}">{{ p.progress|floatformat:0 }}%</div>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  {% empty %}
    <p class="text-muted">No employees found.</p>
  {% endfor %}
</div>
{% endif %}

<div class="dashboard-section-title">Past Performance (Last 6 Months)</div>
<div class="mt-3">
  <a href="{% url 'clients:employee_past_performance' %}" class="dashboard-link">
    View Detailed Past Performance
  </a>
</div>

<div class="dashboard-section-title">Today's Work</div>
<div class="modern-card">
  {% if todays_events %}
    {% for event in todays_events %}
      <div class="d-flex justify-content-between align-items-center border rounded p-2 mb-2" style="background:#f7fafc;">
        <div>
          <strong style="color:#2b6cb0;">{{ event.title }}</strong>
          {% if event.related_prospect %}
            – {{ event.related_prospect.name }}
          {% endif %}
          <div class="mt-1">
            {% if event.related_prospect and event.related_prospect.calling_list_id %}
              <span class="badge bg-info text-dark" title="From Calling List">Calling List</span>
            {% elif event.type == 'birthday' or 'birthday' in event.title|lower %}
              <span class="badge bg-warning text-dark" title="Birthday follow-up">Birthday</span>
            {% else %}
              <span class="badge bg-secondary" title="Manual/other">General</span>
            {% endif %}
          </div>
          <br>
          <span style="color:#718096;">{{ event.scheduled_time|time:"h:i A" }} | {{ event.get_type_display }}</span>
        </div>
        <div>
          <a href="{% url 'clients:mark_done' event.id %}" class="btn btn-sm btn-success" title="Mark Done">✔</a>
          <a href="{% url 'clients:reschedule' event.id %}" class="btn btn-sm btn-warning" title="Reschedule">↻</a>
          <a href="{% url 'clients:skip' event.id %}" class="btn btn-sm btn-danger" title="Skip">✘</a>
        </div>
      </div>
    {% endfor %}
  {% else %}
    <p class="text-muted">No tasks for today 🎉</p>
  {% endif %}
</div>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const counter = document.getElementById("points-counter");
    if (counter) {
      const target = parseFloat(counter.getAttribute("data-points")) || 0;
      let current = 0;
      const step = target / 50;
      function animate() {
        current += step;
        if (current >= target) {
          counter.innerText = Math.round(target);
        } else {
          counter.innerText = Math.round(current);
          requestAnimationFrame(animate);
        }
      }
      animate();
    }

    const thresholds = [25, 50, 75, 100];
    const progressBars = document.querySelectorAll('.modern-progress [data-progress]');
    progressBars.forEach((bar) => {
      const progress = parseFloat(bar.dataset.progress || '0');
      const clamped = Math.min(progress, 100);
      bar.style.width = `${clamped}%`;
      bar.textContent = `${Math.round(progress)}%`;

      if (progress >= 25) {
        bar.classList.add('pulse-glow');
        setTimeout(() => bar.classList.remove('pulse-glow'), 1200);
      }
      thresholds.forEach((t, idx) => {
        if (progress >= t && bar.parentElement) {
          const spark = document.createElement('span');
          spark.className = 'spark-pop';
          spark.style.left = `${Math.min(96, t)}%`;
          bar.parentElement.appendChild(spark);
          setTimeout(() => spark.remove(), 1000 + idx * 60);
        }
      });
    });

    const salaryMeter = document.querySelector('[data-salary-meter]');
    if (salaryMeter) {
      const salary = parseFloat(salaryMeter.dataset.salary || '0');
      const salaryRatio = parseFloat(salaryMeter.dataset.salaryRatio || '0');
      const pointsRatio = parseFloat(salaryMeter.dataset.pointsRatio || '0');
      const salaryFill = salaryMeter.querySelector('.salary-fill');
      if (salary > 0 && salaryFill && pointsRatio >= salaryRatio) {
        salaryFill.classList.add('pulse-glow');
        setTimeout(() => salaryFill.classList.remove('pulse-glow'), 1200);
      }
    }
  });
</script>
{% endblock %}
